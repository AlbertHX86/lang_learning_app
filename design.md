# OpenSpec 设计说明（对齐当前代码基线）

更新时间：2026-04-20
仓库路径：`/Users/bytedance/Desktop/language-learning-platform`

## 1. 目标与约束

本文档用于把设计口径与当前实现严格对齐，避免“文档超前于代码”。

硬约束：
- 当前不存在 `/dashboard` 路由。
- 当前不存在真实后端鉴权（无 JWT/OAuth/后端会话）。
- 当前是前端演示型 SPA，核心交互以本地状态和静态数据为主。

## 2. 现状分析（App / Layout / Home / vite / package）

### 2.1 App 路由现状（`src/App.tsx`）

已实现路由：
- `/`
- `/courses`
- `/courses/:id`
- `/learn/:courseId/:lessonId`
- `/profile`
- `/community`
- `/calendar`
- `/auth/login`
- `/auth/register`

结论：
- 无 `/dashboard`。
- 登录/注册页是独立路由，不走 `Layout`。
- 与 `spec.md` 的“无后端鉴权”目标一致。
- 额外发现：代码含 `/calendar`，而 `spec.md` 当前“范围内路由”未列出该页，属于文档与代码轻度偏差，需在后续规范同步时修订。

### 2.2 Layout 现状（`src/components/Layout.tsx`）

- 包含顶部导航、移动端菜单、页脚、主题切换。
- 导航项：Home / Courses / Profile / Community / Calendar。
- 文案语言混用：站点名中文，但导航和部分页脚为英文。
- 登录入口仅前端跳转到 `/auth/login`，无权限门禁逻辑。

### 2.3 Home 现状（`src/pages/Home.tsx`）

- 首页包含 Hero、语言选择、特性卡片、精选课程、CTA。
- 文案严重混用（中文/英文/西语并存），品牌语气不统一。
- 课程、特性、图片均为页面内静态数据。
- 课程图片来自外链生成服务 URL（第三方可用性与合规风险需单独审查）。

### 2.4 vite 配置现状（`vite.config.ts`）

- `build.sourcemap = 'hidden'`。
- 插件：`@vitejs/plugin-react`、`vite-tsconfig-paths`、`vite-plugin-trae-solo-badge`。
- 未发现代理后端、API baseURL 或运行时鉴权相关配置。

### 2.5 package 现状（`package.json`）

- 栈：React 18 + TypeScript + Vite + Tailwind。
- 脚本：`dev/build/lint/preview/check`。
- 未引入 axios/swr/react-query 等请求层依赖。
- 说明：项目能力边界仍是“前端演示”，非“前后端一体应用”。

## 3. 设计方案：首页中文化与文案治理

### 3.1 设计目标（MVP）

在不引入 i18n 框架、不改动业务流的前提下，完成首页与全局框架的中文化一致性，建立可落地的文案治理机制。

### 3.2 MVP 范围（本期）

1. 首页中文统一
- Hero 主副标题、按钮、课程卡、特性区、CTA 全量中文化。
- 统一术语：课程、学习路径、社区、登录、注册、免费开始。
- 清理混语和夸张营销词（保留真实可交付承诺）。

2. Layout 文案统一
- 导航、页脚、主题切换、快速链接、语言列表统一中文风格。
- 品牌名“学习助手”保持不变。

3. 文案治理基线
- 新建（或约定）`src/content/copy.ts`（或同等集中常量文件）存放首页与布局核心文案。
- 页面不再散落硬编码营销文本；优先从文案常量读取。
- 文案变更通过 PR 审核项管控（见第 5 节检查清单）。

### 3.3 后续范围（非 MVP）

- 引入正式 i18n 方案（如 `react-intl` / `i18next`）。
- 多语言切换与回退机制。
- 文案 A/B 实验与埋点闭环。

## 4. 安全审计方法（适配“纯前端演示”阶段）

### 4.1 审计目标

在“无真实后端鉴权”的前提下，确认：
- 没有伪装成真实安全能力。
- 没有意外泄露配置或敏感信息。
- 为后续接入真实后端预留明确边界。

### 4.2 审计步骤

1. 路由与鉴权声明审计
- 检查是否存在受保护路由守卫（当前无）。
- 检查登录注册是否仅前端校验+跳转（当前是）。
- 结论必须写入文档：演示逻辑不等于生产安全。

2. 数据落盘审计
- 检查 `localStorage/sessionStorage` 使用点。
- 当前已知持久化：`theme`、`calendar-events`（zustand persist）。
- 要求：不得写入 token、密码、私钥。

3. 外部资源与供应链审计
- 清点第三方图片/资源域名。
- 检查构建插件及其生产行为（如徽标插件、外链跳转）。

4. 配置暴露审计
- 全仓检索 `import.meta.env`、`process.env`、`secret|token|key|password`。
- 确认无硬编码密钥与无误暴露到前端 bundle 的私密配置。

5. 结论分级
- P0：真实密钥泄露、可被利用的认证误导。
- P1：长期敏感数据落盘、外链供应链高风险。
- P2：文档与实现不一致导致错误预期。

## 5. Skills / API / 配置暴露检查清单

说明：本清单用于 PR 自检与发布前验收。

### 5.1 Skills（能力声明）检查
- [ ] 文档不声明“已接入真实后端鉴权”。
- [ ] 文档不声明“已实现支付/订阅/权限系统”。
- [ ] 演示能力与代码一致：仅前端校验与本地状态。
- [ ] 若新增能力（如日历持久化），同步更新 `spec.md` 与设计文档。

### 5.2 API 检查
- [ ] 代码中无真实业务 API 调用（`fetch/axios`）或其行为已被明确标注为 Mock。
- [ ] 无 `Authorization: Bearer ...` 之类认证头逻辑。
- [ ] 无 token 刷新、会话续期、权限中间件伪实现。
- [ ] 外链仅用于静态展示资源，不承载敏感业务数据。

### 5.3 配置暴露检查
- [ ] 无 `import.meta.env` 私密变量直接注入并渲染到 UI。
- [ ] 无硬编码密钥（`API_KEY`、`SECRET`、私有证书）。
- [ ] `localStorage/sessionStorage` 不保存密码、token、邮箱+密码组合。
- [ ] 生产构建产物不包含调试敏感信息（已启用 hidden sourcemap，仍需发版检查）。

## 6. 数据流与边界定义

### 6.1 当前数据流（MVP）

- UI 输入 -> React 组件本地状态（`useState`） -> 条件渲染/路由跳转。
- 主题状态：`useTheme` <-> `localStorage(theme)`。
- 日历事件：`zustand persist` <-> `localStorage(calendar-events)`。
- 页面内容：静态数组常量（课程/帖子/学习内容），不经过后端。

### 6.2 边界声明

- 无服务端数据边界：不存在“客户端-业务后端”可信链路。
- 无身份边界：登录注册仅表单校验与页面跳转，不建立真实身份。
- 无权限边界：所有页面默认可访问（除路由参数不存在时的前端兜底）。

### 6.3 后续接入边界（非 MVP）

- 仅在新增 `apiClient + authService + route guard` 后，才可声明“具备基础鉴权能力”。
- 真实用户数据接入前，必须补齐隐私、加密传输、账号注销与数据删除策略。

## 7. 回滚与测试策略

### 7.1 回滚策略

1. 文案/样式回滚
- 首页和布局改动应集中在少量文件（页面 + 文案常量）。
- 每次改动保持单一主题 PR，便于整包回退。

2. 能力声明回滚
- 若发现文档超前（例如误写“已接后端”），优先回滚文档声明，再回滚 UI 暗示文案。

3. 风险控制
- 禁止在文案 PR 中夹带架构行为变更（如新增鉴权逻辑）。

### 7.2 测试策略（MVP）

1. 静态检查
- `npm run check`
- `npm run lint`
- `npm run build`

2. 关键路径手测
- 首页 -> 课程列表 -> 课程详情 -> 学习页。
- 登录/注册表单校验与跳转。
- 主题切换后刷新保持。
- 移动端菜单开关与导航可达。

3. 审计回归
- 执行第 5 节检查清单。
- 特别确认：仍无 `/dashboard`、仍无真实后端鉴权。

## 8. 阶段边界（MVP vs 后续）

### 8.1 MVP（当前必须达成）

- 与现代码一致的设计文档和能力声明。
- 首页中文化与文案统一，不改变既有业务流程。
- 可执行的安全审计方法与检查清单。
- 明确数据流边界与回滚/测试方案。

### 8.2 后续（本期不承诺）

- 真实后端 API 接入。
- 真正的认证授权体系（JWT/OAuth/RBAC/会话刷新）。
- 多语言 i18n 系统化建设。
- 生产级监控、审计日志、风控策略。

---

对齐结论（必须持续成立）：
- 无 `/dashboard`。
- 无真实后端鉴权。
- 任何新增能力必须先更新 `spec.md` 与本文档后再对外宣称。
