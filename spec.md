# OpenSpec - Language Learning Platform（Frontend Baseline）

## 1. 背景
当前仓库是 React + TypeScript + Vite 的前端单页应用（SPA），已具备首页、课程、学习、社区、日历、登录/注册等页面路由与基础交互。

现状边界（以代码为准）：
- 仅前端实现，无后端 API、无服务端鉴权、无数据库持久化。
- 路由中不存在 `/dashboard`。
- 状态管理以现有 `useTheme`（主题切换+localStorage）为主，并已存在可扩展的 Zustand 用例（`useCalendarStore`）；本规范不采用 Context 主导方案。
- 首页与 Layout 可见文案存在中英西混杂，影响一致性与可维护性。

## 2. 目标
1. 完成首页与首页可见布局（Header/Footer/导航/CTA）中文化规范，统一文案与语义。
2. 建立前端风险治理基线，覆盖：`skills` 暴露、API 暴露、敏感信息、构建配置。
3. 形成可执行、可验收的文档交付物，确保后续实现与审查有统一依据。

## 3. 范围
### 3.1 In Scope
1. 首页（`/`）及首页可见布局元素的中文化规范：
   - Hero 文案、语言选择器、推荐课程区、CTA 区。
   - Layout 中 Header 导航、移动端菜单、Footer 可见文案。
2. 前端风险治理规范：
   - `skills` 类内容在前端可见面的暴露约束。
   - 外部 API/第三方资源 URL 暴露与白名单治理。
   - 敏感信息（密钥、凭证、隐私数据）治理。
   - Vite 构建配置与生产暴露面治理。
3. 文档交付规范（spec/design/checklist）。

### 3.2 Out of Scope
1. 新增后端能力、真实登录注册、会话系统、数据库改造。
2. 新增或改造 `/dashboard` 路由（当前不存在，且不在本次范围）。
3. 引入 Context 作为主状态架构重构。
4. 课程业务逻辑重写、社区功能扩展、支付或权限系统。

## 4. 用户故事
1. 作为访客，我希望首页和导航文案统一为中文，以便快速理解产品价值和入口。
2. 作为产品/开发成员，我希望前端暴露面有清晰边界，避免将敏感信息或内部规则打包到浏览器端。
3. 作为维护者，我希望有一份可验收的规范文档，用于评审、联调和发布前检查。

## 5. 功能需求（FR）
1. FR-001（首页中文化）：首页所有对外展示主文案应统一为中文表达，不出现同层级多语言混杂（品牌词或专有名词除外）。
2. FR-002（布局中文化）：Layout 中 Header、导航项、移动菜单、Footer 可见文案应统一中文。
3. FR-003（术语一致性）：同一概念仅保留一套术语（例如“课程/学习/社区/登录”），避免页面间同义词漂移。
4. FR-004（状态基线）：文案与主题状态继续沿用 `useTheme` 机制；跨页可扩展状态沿用 Zustand 思路，不引入 Context 主导方案。
5. FR-005（skills 暴露治理）：前端可见 `skills`（如学习技能标签、能力描述）仅允许展示型字段；禁止在前端存放内部评分权重、反作弊策略、审核规则等敏感业务逻辑细节。
6. FR-006（API 暴露治理）：前端仅可暴露必要的公开接口地址与资源 URL；第三方域名需登记并可审计。
7. FR-007（敏感信息治理）：仓库与构建产物中不得出现 API Key、Secret、私有令牌、真实账号密码、私密个人数据样例。
8. FR-008（构建配置治理）：生产构建必须保持最小暴露原则（校验 sourcemap 策略、开发调试能力、插件产物可见面）。
9. FR-009（文档交付）：输出一份覆盖背景/范围/FR/NFR/安全/验收/交付物的规范文档，并与当前代码事实一致。

## 6. 非功能需求（NFR）
1. NFR-001（一致性）：同一页面信息层不混用中文、英文、西语表达；文案风格统一。
2. NFR-002（可维护性）：文案规范可映射到现有组件结构（`src/pages/Home.tsx`、`src/components/Layout.tsx`），便于后续迭代。
3. NFR-003（可扩展性）：状态治理不破坏现有 `useTheme`，并保留 Zustand 扩展空间。
4. NFR-004（可审计性）：风险治理项必须可通过静态扫描与配置检查落地。
5. NFR-005（真实性）：文档不得声明未实现能力（后端、真实鉴权、`/dashboard`、服务端安全能力等）。

## 7. 安全与合规
1. 当前系统为纯前端演示形态，任何前端代码与打包产物均可被用户查看；因此前端数据默认视为“公开数据”。
2. `skills`、题目、规则说明等内容若放在前端，仅能承载展示信息，不可承载需要保密的策略参数。
3. 外部资源（如第三方图片 URL）视为外部 API 暴露面，需建立来源清单、可用性与合规评估。
4. 敏感信息治理要求：
   - 禁止硬编码密钥/令牌/私密配置。
   - 环境变量采用最小化公开策略；即便 `VITE_` 变量也不得存放敏感值。
   - 发布前执行关键字扫描（如 `secret|token|key|password`）与人工复核。
5. 构建与发布治理要求：
   - 校验 Vite 生产配置（含 sourcemap 策略）符合最小暴露原则。
   - 生产包不引入仅用于开发定位/调试的高风险能力。
   - 对新增构建插件进行供应链风险评估与版本锁定策略。

## 8. 验收标准
1. 文案验收：
   - 首页与 Layout 可见文案完成中文化规范定义。
   - 导航、按钮、区块标题和说明文案术语一致。
2. 事实验收：
   - 文档明确“React+Vite 前端、无后端、无 `/dashboard`”。
   - 文档明确状态基线为 `useTheme` + 可扩展 Zustand，且非 Context 主导。
3. 风险验收：
   - 形成 `skills/API/敏感信息/构建配置` 四类治理条目。
   - 每类条目具备可执行检查点（可扫描或可人工审计）。
4. 交付验收：
   - `spec.md` 结构完整，包含：背景、目标、范围（in/out）、用户故事、FR/NFR、安全合规、验收标准、交付物。
   - 内容不虚构已实现能力，不与仓库现状冲突。

## 9. 交付物
1. `spec.md`（本规范，覆盖本次约束范围）。
2. 风险治理检查清单（内嵌于规范第 7/8 节，可直接用于发布前审查）。
3. 文案中文化实施基线（以内文 FR 与验收项作为执行依据，指向首页与 Layout 可见层）。
